module.exports = (app, router)=>{
    /*
    @usage

    router.sub(uri, ...middleware)
    
    uri = '/example/:id' ;
    middleware = [
        async function(ctx, next){...},
        app.middleware.example,
        'example.index', // controller and action
    ]
    
    */

}